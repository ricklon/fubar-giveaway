{
  "name": "fubar-giveaway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "booth": "npm run build && vite preview --host 127.0.0.1 --port 4174 --strictPort",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/drawing.test.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^7.1.7"
  }
}
